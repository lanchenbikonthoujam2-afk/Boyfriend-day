// This is the file where you will add interactivity!

document.addEventListener('DOMContentLoaded', () => {
    // A simple test to make sure the script is linked
    console.log('Script file is running!');

    // Example of a function that runs when the page loads:
    alert('A little message to surprise him when he opens the page!');
});
